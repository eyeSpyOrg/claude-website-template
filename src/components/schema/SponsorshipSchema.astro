---
/**
 * SponsorshipSchema.astro — placeholder for corporate sponsorship structured data.
 *
 * The Fashion PAWty event-specific sponsorship tiers have been removed.
 * If ESF adds corporate sponsorship packages in future, rebuild this component
 * with an ItemList of Offers scoped to the relevant program or event.
 *
 * Usage: <SponsorshipSchema tiers={tiersArray} url="/programs/sponsorship/" />
 * Validate output at https://search.google.com/test/rich-results
 */

/* Component intentionally renders nothing until sponsor data is defined. */
---
<!-- SponsorshipSchema: no active sponsor tiers — add tiers prop to activate -->
