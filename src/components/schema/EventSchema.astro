---
/**
 * EventSchema — JSON-LD Event schema for a single event.
 * Import and use on an event page when you have event details.
 * Data comes from SITE.event (src/config/site.js).
 * Validate at https://search.google.com/test/rich-results before launch.
 *
 * NOTE: SITE.event is a placeholder — populate it in site.js before use.
 */
import JsonLd from './JsonLd.astro';
import { SITE } from '../../config/site.js';
---
<!-- EventSchema: add SITE.event to site.js and use on event pages -->
